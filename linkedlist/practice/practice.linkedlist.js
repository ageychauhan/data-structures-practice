class PracticeLinkedList {
    constructor () {
        this.head = null;
    }

    findMiddleElementInLinkedList(name) {
        if (!this.head) {
            return null;
        }

        let currentNode = this.head;
        
    }
}